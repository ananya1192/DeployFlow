apiVersion: v1
kind: Service
metadata:
  name: {{ include "deployflow.fullname" . }}
  labels:
    {{- include "deployflow.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "deployflow.selectorLabels" . | nindent 4 }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http